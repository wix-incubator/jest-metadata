{
  "name": "@jest-metadata/recorder",
  "description": "Fixture recorder for testing jest-metadata with a wide range of Jest versions",
  "private": true,
  "version": "1.0.0-beta.10",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "is-ci": "^3.0.1",
    "jest": "latest",
    "jest-metadata": "^1.0.0-beta.10",
    "semver": "^7.5.4"
  },
  "scripts": {
    "build:dev": "cd ../library && npm run build && npm run instrument && cd - && npm run build",
    "build": "npm run build:env-1 && npm run build:env-N && npm run build:no-env-1 && npm run build:no-env-N",
    "build:env-1": "cross-env PRESET=env-1 jest || true",
    "build:env-N": "cross-env PRESET=env-N jest || true",
    "build:no-env-1": "cross-env PRESET=no-env-1 jest || true",
    "build:no-env-N": "cross-env PRESET=no-env-N jest || true"
  }
}
