{
  "name": "@jest-metadata/recorder",
  "description": "Fixture recorder for testing jest-metadata with a wide range of Jest versions",
  "private": true,
  "version": "1.0.0-beta.10",
  "dependencies": {
    "bunyamin": "^1.0.1",
    "jest": "latest",
    "jest-metadata": "^1.0.0-beta.10",
    "semver": "^7.5.4"
  },
  "scripts": {
    "pretest": "cd ../library && npm run build",
    "build": "npm run build:env-1 && npm run build:env-N && npm run build:no-env-1 && npm run build:no-env-N",
    "build:env-1": "jest -c env.config.js -i || true",
    "build:env-N": "jest -c env.config.js -w 2 || true",
    "build:no-env-1": "jest -c no-env.config.js -i || true",
    "build:no-env-N": "jest -c no-env.config.js -w 2 || true"
  }
}
